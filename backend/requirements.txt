# Litetube — backend Python dependencies
# Install: pip install -r requirements.txt

fastapi>=0.110,<1
uvicorn[standard]>=0.30,<1
aiosqlite>=0.20,<1
bcrypt>=4.0,<5
python-multipart>=0.0.9,<1

# Google Sign-In (Этап 1: feature-flagged via GOOGLE_AUTH_ENABLED, default off)
google-auth>=2.30,<3
# requests is a transitive dep of google-auth; pinned explicitly so CI is offline-deterministic

# Dev / test
pytest>=8,<9
pytest-asyncio>=0.24,<1
httpx>=0.27,<1
eval_type_backport>=0.2,<1
